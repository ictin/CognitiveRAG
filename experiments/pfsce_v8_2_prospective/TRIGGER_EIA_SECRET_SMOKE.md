# Trigger EIA secret smoke test

Transport-only verification that the repository secret `EIA_API_KEY` is present and can authenticate a request to the official EIA API. This does not alter any frozen forecasting protocol, model, gate, or prospective result.
